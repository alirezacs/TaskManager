# Task Manager Frontend

## Setup

1. Copy `.env.example` to `.env` and update the API base URL if needed.
2. Install dependencies:

```bash
npm install
```

3. Start the dev server:

```bash
npm run dev
```

The app will be available at `http://localhost:3000`.
